# Where scripts/update-songs.ts pulls the song database from. Defaults to the
# production admin app, so you only need this for a local admin instance.
ADMIN_EXPORT_URL=https://admin.dainorelis.app/api/export

# Shared secret for that endpoint. Copy it from the admin app's Vercel
# environment (EXPORT_TOKEN).
ADMIN_EXPORT_TOKEN=...
